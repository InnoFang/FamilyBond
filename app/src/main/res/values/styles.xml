<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Translucent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">#0288D1</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <style name="LoginTextInputLayoutStyle">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:paddingEnd">30dp</item>
        <item name="android:paddingStart">50dp</item>
        <item name="android:textColorHint">@color/LoginTextInputLayoutHint</item>
    </style>

    <style name="RegisterTextInputLayoutStyle">
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:paddingEnd">30dp</item>
        <item name="android:paddingStart">50dp</item>
        <item name="android:textColorHint">@color/RegisterTextInputLayoutHint</item>
    </style>

    <style name="LoginEditTextStyle">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:background">@drawable/bg_login_edit_text_selector</item>
        <item name="android:paddingBottom">2dp</item>
    </style>

    <style name="RegisterEditTextStyle">
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textSize">14sp</item>
        <item name="android:background">@drawable/bg_register_edit_text_selector</item>
        <item name="android:paddingBottom">2dp</item>
    </style>


    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
